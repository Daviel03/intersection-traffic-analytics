Place your local clip here, for example:

```text
data/intersection_demo.mp4
```

The project does not bundle a sample video. Update `configs/scenes/intersection_demo.yaml` so the geometry matches your chosen clip.

For this workspace, `data/intersection_demo.mp4` has already been downloaded from Pexels as an initial fixed-camera intersection test clip. See `data/intersection_demo_source.md` for provenance and license notes.
